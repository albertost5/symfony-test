{% extends 'base.html.twig' %}

{% block title %}Empresa
{% endblock %}

{% block body %}

	<div class="container">
		<div class="row mt-5">

			<div class="col">
				<h1>Empresa</h1>
			</div>

		</div>

		<table class="table table-dark">
			<tbody>
				<tr>
					<th scope="col">Id</th>
					<td>{{ empresa.id }}</td>
				</tr>
				<tr>
					<th scope="col">Nombre</th>
					<td>{{ empresa.nombre }}</td>
				</tr>
				<tr>
					<th scope="col">Teléfono</th>
					<td>{{ empresa.telefono }}</td>
				</tr>
				<tr>
					<th scope="col">Email</th>
					<td>{{ empresa.email }}</td>
				</tr>
			</tbody>
		</table>
		{% if "ROLE_ADMIN" in user.roles  %}
			{{ include('empresa/_delete_form.html.twig') }}
		{% endif %}
		<a class="btn btn-primary mt-1" href="{{ path('empresa_index') }}">Cancelar</a>

	{% endblock %}
